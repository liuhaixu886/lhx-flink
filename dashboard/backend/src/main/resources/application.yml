server:
  port: 8088

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://node102:9030/gmall_realtime?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: "123456"
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      connection-timeout: 5000

logging:
  level:
    root: info
    com.lhx.dashboard: info
