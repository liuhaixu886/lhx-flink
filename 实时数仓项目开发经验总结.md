# 实时数仓项目开发经验总结

## 1. 项目背景

本项目实现了一套基于 Flink 的实时数仓链路，围绕 GMall 业务库和日志数据，完成了从数据采集、接入、维度分流、明细分流、窗口汇总到 ADS 查询视图的完整流程。

整体目标不是只把数据搬到 Kafka，而是按数仓分层思想把每一层的职责拆清楚：

- ODS：保持原始数据入口，做最基础的格式保护。
- DIM：识别维表，配置化写入 HBase。
- DWD：做明细层拆分、基础清洗和事实宽表加工。
- DWS：做面向主题的窗口汇总。
- ADS：面向实时看板提供可查询指标。

最终跑通的主链路如下：

```text
MySQL gmall
  -> Flink CDC
  -> Kafka topic_db
  -> ODS ods_db
  -> DWD 表级事实主题
  -> DWD 订单明细事实宽流
  -> DWS SKU 下单窗口汇总
  -> Doris
  -> ADS 查询视图
```

日志链路如下：

```text
Kafka topic_log
  -> ODS ods_log
  -> DWD 日志分流主题
```

## 2. 技术选型

### 2.1 Flink

项目的实时计算核心使用 Flink，主要原因是：

- 支持有状态流处理，可以做订单明细和订单主表的状态关联。
- 支持事件时间和窗口计算，可以做 30 秒滚动窗口汇总。
- 支持 Checkpoint，可以保证作业失败后从一致状态恢复。
- 与 Kafka、CDC、Doris 等生态组件集成成熟。

项目中统一设置了：

```java
env.enableCheckpointing(5000L, CheckpointingMode.EXACTLY_ONCE);
env.setRestartStrategy(RestartStrategies.fixedDelayRestart(3, 3000L));
env.setStateBackend(new HashMapStateBackend());
```

这套配置保证了 Kafka 消费位点、状态和下游写入能有基本的容错能力。

### 2.2 Flink CDC

业务数据采集使用 Flink CDC，而不是定时 JDBC 查询，原因是：

- Flink CDC 基于 MySQL Binlog，能实时捕获新增、修改、删除。
- 不需要频繁扫表，避免给业务库造成过大压力。
- 能保留 Debezium 变更结构，例如 `before`、`after`、`op`、`source.table`。
- 可以支持初始化快照加增量同步，适合实时数仓的 ODS 接入。

核心代码在 `ods_db_mysql_cdc_kafka.java`：

```java
Properties debeziumProperties = new Properties();
debeziumProperties.setProperty("decimal.handling.mode", "string");

MySqlSource.<String>builder()
        .hostname("node101")
        .port(3306)
        .databaseList("gmall")
        .deserializer(new JsonDebeziumDeserializationSchema())
        .debeziumProperties(debeziumProperties)
        .build();
```

其中 `decimal.handling.mode=string` 是项目中非常关键的经验点。没有这个配置时，MySQL `DECIMAL` 类型会被 Debezium 序列化成类似 `CSoY`、`AA==` 的异常字符串，后续 DWD 和 DWS 金额计算会全部出问题。

### 2.3 Kafka

Kafka 作为实时数仓的消息总线，承担各层之间的解耦。

本项目主要主题：

```text
topic_db                         CDC 原始业务数据
topic_log                        原始日志数据
ods_db                           ODS 合法业务 JSON
ods_log                          ODS 合法日志 JSON
dirty_data                       统一脏数据主题
dwd_trade_order_detail           订单明细表级 DWD
dwd_trade_order_info             订单主表表级 DWD
dwd_trade_order_detail_activity  订单活动明细 DWD
dwd_trade_order_detail_coupon    订单优惠券明细 DWD
dwd_trade_order_detail_fact      订单明细事实宽流
```

Kafka 的作用不是只缓存数据，而是把 ODS、DIM、DWD、DWS 作业解耦。某个作业重启，不会影响上游继续运行。

### 2.4 HBase

DIM 层使用 HBase 保存维度数据，原因是：

- 维表查询通常按主键 rowkey 点查，HBase 适合这种访问模式。
- HBase 支持维度数据不断更新。
- DWD/DWS 后续做维度关联时，可以根据 rowkey 快速读取维度属性。

项目中 HBase 规范：

```text
namespace: gmall_dim
family: info
rowkey: 配置表 sink_row_key
table: 配置表 sink_table
```

### 2.5 Doris

DWS 和 ADS 使用 Doris，原因是：

- Doris 适合实时明细汇总和 OLAP 查询。
- 支持 Stream Load，Flink 可以实时写入。
- 查询延迟低，适合实时大屏、TopN、趋势图等 ADS 场景。
- 比直接查 Kafka 或 HBase 更适合做聚合查询。

本项目 Doris 表：

```text
gmall_realtime.dws_trade_sku_order_window
```

写入方式使用 Doris Flink Connector：

```java
DorisOptions dorisOptions = DorisOptions.builder()
        .setFenodes("node102:8030")
        .setTableIdentifier("gmall_realtime.dws_trade_sku_order_window")
        .setUsername("root")
        .setPassword("123456")
        .build();
```

这里要特别注意：`fenodes` 使用的是 Doris FE HTTP 端口 `8030`，不是 MySQL 查询端口 `9030`。

## 3. Maven 依赖

项目核心依赖包括：

- Flink 1.13.1
- Scala 2.12
- Flink CDC 2.1.1
- Kafka Connector
- Fastjson
- HBase Client 2.1.0
- Doris Flink Connector 1.0.3
- SLF4J + Log4j

后续遇到 Doris Connector 依赖冲突时，显式补充了：

```xml
<dependency>
    <groupId>org.apache.httpcomponents</groupId>
    <artifactId>httpclient</artifactId>
    <version>4.5.13</version>
</dependency>

<dependency>
    <groupId>org.apache.httpcomponents</groupId>
    <artifactId>httpcore</artifactId>
    <version>4.4.13</version>
</dependency>
```

原因是 HBase/Hadoop 会传递旧版 `httpclient-4.2.5`，而 Doris Connector 需要 `HttpRequestBase.setConfig()`，旧版本没有这个方法。

## 4. ODS 层实现

ODS 层的目标是保证数据原样进入实时数仓，同时做最基础的格式保护。

### 4.1 业务 ODS

业务采集分两步：

1. `ods_db_mysql_cdc_kafka.java`
   - Flink CDC 读取 MySQL `gmall`
   - 写入 Kafka `topic_db`

2. `ods_db_sink.java`
   - 消费 `topic_db`
   - 打印原始数据
   - 判断是否为合法 JSON
   - 合法写入 `ods_db`
   - 非法写入 `dirty_data`

ODS 层只做 JSON 合法性校验，不做业务字段清洗，不拆表。

核心模式：

```java
private static final OutputTag<String> DIRTY_DATA_TAG = new OutputTag<String>("dirty-data") {};

sourceStream.process(new ProcessFunction<String, String>() {
    @Override
    public void processElement(String value, Context context, Collector<String> collector) {
        if (isValidJson(value)) {
            collector.collect(value);
        } else {
            context.output(DIRTY_DATA_TAG, value);
        }
    }
});
```

### 4.2 日志 ODS

日志 ODS 由 `ods_log_sink.java` 实现：

- 消费 `topic_log`
- 打印原始日志
- 合法 JSON 写入 `ods_log`
- 非法 JSON 写入 `dirty_data`

这样可以保证后续 DWD 只处理格式正确的数据，脏数据不会污染明细层。

## 5. DIM 层实现

DIM 层由 `dim_db_sink.java` 实现，核心思想是配置化维表分流。

### 5.1 配置表

MySQL 配置表：

```sql
CREATE TABLE realtime_config.table_process_dim (
  source_table varchar(200) NOT NULL,
  sink_table varchar(200) NOT NULL,
  sink_family varchar(200) NOT NULL DEFAULT 'info',
  sink_row_key varchar(200) NOT NULL DEFAULT 'id',
  sink_columns varchar(1000) DEFAULT NULL,
  sink_type varchar(20) NOT NULL DEFAULT 'dim',
  PRIMARY KEY (source_table)
);
```

这个配置表决定：

- 哪些业务表是维表。
- 写入 HBase 哪张表。
- rowkey 取哪个字段。
- 写哪些列。
- 列族是什么。

配置 SQL 在：

```text
src/main/resources/sql/table_process_dim.sql
```

### 5.2 配置流

DIM 作业使用 Flink CDC 读取配置表：

```java
MySqlSource.<String>builder()
        .databaseList("realtime_config")
        .tableList("realtime_config.table_process_dim")
        .deserializer(new JsonDebeziumDeserializationSchema())
        .build();
```

配置流广播到主流：

```java
BroadcastStream<String> configBroadcastStream =
        configStream.broadcast(TABLE_PROCESS_DIM_STATE_DESCRIPTOR);
```

这样配置表新增、修改、删除后，作业可以动态调整维表分流逻辑，不需要修改代码。

### 5.3 主流处理

DIM 主流消费 `ods_db`：

- 从 Debezium JSON 中取 `source.table`。
- 根据广播配置判断是否为维表。
- `c/r/u` 取 `after` 写 HBase。
- `d` 取 `before` 删除 HBase 行。
- 按 `sink_columns` 裁剪字段。
- rowkey 字段只作为行键，不重复写入列族。

HBase 表不存在时自动创建。

项目中已经配置了 24 张维表，例如：

```text
dim_base_province
dim_base_region
dim_sku_info
dim_spu_info
dim_base_dic
dim_user_info
dim_coupon_info
```

## 6. DWD 层实现

DWD 层负责明细数据清洗、分流和事实加工。

### 6.1 日志 DWD

`dwd_log_split.java` 消费 `ods_log`，做基础清洗和日志分流。

清洗规则：

- 空字符串或非法 JSON 写入 `dirty_data`。
- 缺少 `common` 或 `ts` 写入 `dirty_data`。
- 缺少 `common.mid` 写入 `dirty_data`。
- 同时缺少 `page` 和 `start` 写入 `dirty_data`。

分流主题：

```text
dwd_traffic_page_log
dwd_traffic_start_log
dwd_traffic_display_log
dwd_traffic_action_log
dwd_traffic_error_log
```

曝光、动作、错误日志会补充 `common`、`page`、`ts`，保证后续分析可追溯。

### 6.2 业务 DWD

`dwd_db_split.java` 消费 `ods_db`，解析 Debezium JSON。

清洗规则：

- 非法 JSON 写入 `dirty_data`。
- 缺少 `source.table` 或 `op` 写入 `dirty_data`。
- `c/r/u` 缺少 `after` 写入 `dirty_data`。
- 删除事件第一版忽略。
- 维表不进入 DWD，由 DIM 负责。

表级事实主题：

```text
cart_info -> dwd_trade_cart_info
order_info -> dwd_trade_order_info
order_detail -> dwd_trade_order_detail
order_detail_activity -> dwd_trade_order_detail_activity
order_detail_coupon -> dwd_trade_order_detail_coupon
payment_info -> dwd_trade_payment_info
order_refund_info -> dwd_trade_order_refund_info
refund_payment -> dwd_trade_refund_payment
order_status_log -> dwd_trade_order_status_log
coupon_use -> dwd_tool_coupon_use
favor_info -> dwd_interaction_favor_info
comment_info -> dwd_interaction_comment_info
```

DWD 输出会补充元字段：

```text
_source_table
_op
_ts_ms
```

### 6.3 订单明细事实宽流

`dwd_trade_order_detail_fact.java` 基于 DWD 表级主题继续加工，不回读 ODS。

输入主题：

```text
dwd_trade_order_detail
dwd_trade_order_info
dwd_trade_order_detail_activity
dwd_trade_order_detail_coupon
```

输出主题：

```text
dwd_trade_order_detail_fact
```

关联规则：

- `order_detail.order_id = order_info.id`
- `order_detail_activity.order_detail_id = order_detail.id`
- `order_detail_coupon.order_detail_id = order_detail.id`

使用 `KeyedCoProcessFunction` 和 `ValueState` 实现状态关联：

```java
private ValueState<JSONObject> orderDetailState;
private ValueState<JSONObject> orderInfoState;
```

状态 TTL 设置为 30 分钟，避免状态无限增长。

事实宽流输出字段包括：

```text
id
order_id
sku_id
sku_name
order_price
sku_num
split_total_amount
split_activity_amount
split_coupon_amount
create_time
user_id
province_id
order_status
payment_way
original_total_amount
activity_id
activity_rule_id
coupon_id
coupon_use_id
_fact_type
_join_ts
```

这个事实宽流是后续 DWS 交易主题汇总的基础。

## 7. DWS 层实现

DWS 第一版实现了 SKU 下单窗口汇总，类名：

```text
com.lhx.D_DWS.dws_trade_sku_order_window
```

输入：

```text
dwd_trade_order_detail_fact
```

输出：

```text
Doris: gmall_realtime.dws_trade_sku_order_window
dirty_data
```

### 7.1 窗口逻辑

DWS 使用事件时间：

- 优先取 `create_time`。
- 无法解析时使用当前处理时间。
- 允许 3 秒乱序。
- 按 `sku_id` 分组。
- 30 秒滚动窗口。

核心代码：

```java
factStream
    .assignTimestampsAndWatermarks(
        WatermarkStrategy
            .<SkuOrderFact>forBoundedOutOfOrderness(Duration.ofSeconds(3))
            .withTimestampAssigner((element, recordTimestamp) -> element.eventTime)
    )
    .keyBy(SkuOrderFact::getSkuId)
    .window(TumblingEventTimeWindows.of(Time.seconds(30)))
    .process(new SkuOrderWindowFunction());
```

聚合指标：

```text
order_amount = sum(split_total_amount)
order_count = 窗口内 SKU 明细行数，不对 order_id 去重
sku_num = sum(sku_num)
```

这里根据项目口径，`order_count` 表示 SKU 明细粒度下单次数，不做 `order_id` 去重。

### 7.2 写入 Doris

Doris DDL 在：

```text
src/main/resources/sql/dws_doris.sql
```

表结构：

```sql
CREATE TABLE IF NOT EXISTS gmall_realtime.dws_trade_sku_order_window (
  sku_id varchar(50),
  stt datetime,
  edt datetime,
  sku_name varchar(500),
  cur_date date,
  order_amount decimal(16,2),
  order_count bigint,
  sku_num bigint,
  ts bigint
)
DUPLICATE KEY(sku_id, stt, edt)
DISTRIBUTED BY HASH(sku_id) BUCKETS 10
PROPERTIES (
  "replication_num" = "1"
);
```

Doris Duplicate Key 有一个重要规则：Key 字段必须是表字段声明的有序前缀。因此字段顺序必须是：

```text
sku_id, stt, edt, ...
```

不能写成：

```text
sku_id, sku_name, stt, edt, ...
```

否则会报：

```text
Key columns should be a ordered prefix of the schema
```

### 7.3 Doris 写入格式经验

Doris Connector 1.0.3 使用 DataStream `String` 写 JSON 时，会把批次转成 `List.toString()` 的形式，导致 Doris 解析成坏数据并报：

```text
[DATA_QUALITY_ERROR] too many filtered rows
```

最终采用 TSV 文本写入：

```java
streamLoadProperties.setProperty("format", "csv");
streamLoadProperties.setProperty("column_separator", "\t");
streamLoadProperties.setProperty("line_delimiter", "\n");
streamLoadProperties.setProperty("columns", "sku_id,stt,edt,sku_name,cur_date,order_amount,order_count,sku_num,ts");
```

并手动构造 TSV 行：

```java
collector.collect(buildDorisCsvLine(
        skuId,
        stt,
        edt,
        skuName,
        curDate,
        amount,
        String.valueOf(orderCount),
        String.valueOf(skuNum),
        String.valueOf(System.currentTimeMillis())
));
```

最终写入成功日志：

```text
"Status": "Success"
"NumberTotalRows": 34
"NumberLoadedRows": 34
"NumberFilteredRows": 0
```

## 8. ADS 层实现

ADS 第一版不新增 Flink 作业，而是在 Doris 中基于 DWS 表创建视图。

SQL 文件：

```text
src/main/resources/sql/ads_doris.sql
```

视图包括：

```text
ads_trade_stats_today
ads_trade_sku_order_topn_today
ads_trade_trend_30s
ads_trade_hour_stats_today
```

### 8.1 今日交易总览

```sql
SELECT
  cur_date,
  SUM(order_amount) AS gmv,
  SUM(order_count) AS sku_order_count,
  SUM(sku_num) AS sku_num,
  COUNT(DISTINCT sku_id) AS ordered_sku_count
FROM gmall_realtime.dws_trade_sku_order_window
WHERE cur_date = CURRENT_DATE()
GROUP BY cur_date;
```

### 8.2 SKU TopN

```sql
SELECT
  cur_date,
  sku_id,
  sku_name,
  order_amount,
  sku_order_count,
  sku_num,
  ROW_NUMBER() OVER (PARTITION BY cur_date ORDER BY order_amount DESC) AS rn
FROM (...)
```

ADS 层直接面向看板和报表查询，避免应用层重复写复杂聚合 SQL。

## 9. 作业启动顺序

推荐启动顺序：

```text
1. 执行 MySQL 配置表 SQL
2. 执行 Doris DWS/ADS SQL
3. 启动 ods_db_mysql_cdc_kafka
4. 启动 ods_db_sink
5. 启动 ods_log_sink
6. 启动 dim_db_sink
7. 启动 dwd_db_split
8. 启动 dwd_log_split
9. 启动 dwd_trade_order_detail_fact
10. 启动 dws_trade_sku_order_window
```

核心运行命令示例：

```bash
flink run -c com.lhx.A_ODS.ods_db_mysql_cdc_kafka target/zg6-flink-warehouse-1.0-SNAPSHOT.jar
flink run -c com.lhx.A_ODS.ods_db_sink target/zg6-flink-warehouse-1.0-SNAPSHOT.jar
flink run -c com.lhx.B_DIM.dim_db_sink target/zg6-flink-warehouse-1.0-SNAPSHOT.jar
flink run -c com.lhx.C_DWD.dwd_db_split target/zg6-flink-warehouse-1.0-SNAPSHOT.jar
flink run -c com.lhx.C_DWD.dwd_trade_order_detail_fact target/zg6-flink-warehouse-1.0-SNAPSHOT.jar
flink run -c com.lhx.D_DWS.dws_trade_sku_order_window target/zg6-flink-warehouse-1.0-SNAPSHOT.jar
```

## 10. 遇到的难点和解决经验

### 10.1 Flink CDC 缺少 TableException

问题：

```text
NoClassDefFoundError: org/apache/flink/table/api/TableException
```

原因是 Flink CDC 运行时依赖 Flink Table 相关类。

解决：

在 Maven 中补充 `flink-table-common` 等依赖。

### 10.2 Kafka ThreadUtils 缺失

问题：

```text
NoClassDefFoundError: org/apache/kafka/common/utils/ThreadUtils
```

原因是 Flink CDC 2.1.1 内部 Debezium 依赖 Kafka 2.7 相关类，而项目原来的 Kafka Client 版本偏低。

解决：

显式指定：

```xml
<kafka-clients.version>2.7.0</kafka-clients.version>
```

### 10.3 中文乱码

问题包括：

- Kafka 写入中文显示异常。
- PowerShell 查看 UTF-8 文件时中文注释显示乱码。

解决经验：

- Kafka 读写统一使用 UTF-8 `SimpleStringSchema`。
- IDEA 运行参数加 `-Dfile.encoding=UTF-8`。
- 文件保存为 UTF-8。
- PowerShell 控制台乱码不一定代表文件本身乱码，更多时候是终端编码显示问题。

### 10.4 MySQL DECIMAL 被写成异常字符串

问题：

ODS 中金额字段出现：

```text
"total_amount": "CSoY"
"activity_reduce_amount": "AA=="
```

原因是 Debezium 对 Decimal 的默认处理方式不是普通字符串。

解决：

```java
debeziumProperties.setProperty("decimal.handling.mode", "string");
```

这是本项目最关键的 CDC 经验之一。实时数仓中金额字段必须在入口处保证格式正确，否则 DWD/DWS 再清洗也只是补救。

### 10.5 Doris FE 端口用错

问题：

一开始把 Doris 连接参数配置成 `node102:9030`。

原因：

`9030` 是 Doris MySQL 查询端口，不是 Stream Load HTTP 端口。

解决：

Flink Doris Connector 使用：

```text
node102:8030
```

查询 Doris 使用：

```text
node102:9030
```

### 10.6 Doris 建表 Key 顺序错误

问题：

```text
Key columns should be a ordered prefix of the schema
```

原因是 Doris 要求 Duplicate Key 字段必须是表字段声明的有序前缀。

解决：

把字段声明顺序改成：

```text
sku_id, stt, edt, sku_name, ...
```

### 10.7 Doris Connector 与 HttpClient 冲突

问题：

```text
NoSuchMethodError: HttpRequestBase.setConfig
```

原因：

Hadoop/HBase 传递了旧版 `httpclient-4.2.5`，Doris Connector 需要新版方法。

解决：

显式加入：

```xml
httpclient 4.5.13
httpcore 4.4.13
```

### 10.8 Doris Stream Load 数据质量错误

问题：

```text
[DATA_QUALITY_ERROR] too many filtered rows
```

原因：

Doris Connector 1.0.3 在 DataStream String JSON 模式下，批量写入格式不是标准 JSON Lines。

解决：

改成 CSV/TSV 写入，并显式指定 columns。

### 10.9 表中没有数据但作业正常

现象：

Flink 作业一直 Checkpoint 成功，但 Doris 表没有新增数据。

排查思路：

- 先看是否有 `Streamload Response Success`。
- 再看 DWS 控制台是否打印窗口结果。
- 再看 Kafka 消费组 offset 是否已经消费到最新。
- 如果没有新事实数据，窗口不会触发，自然不会写 Doris。

最终经验：

不要只看作业是否 RUNNING。实时链路排查要按顺序看：

```text
源头是否有新数据
Kafka 主题是否有数据
Flink 是否消费到数据
窗口是否触发
Doris Stream Load 是否成功
Doris 查询是否能查到
```

## 11. 项目收获

这次开发最大的收获是把实时数仓每一层职责真正落到了代码里。

ODS 层保持原始入口，只做格式保护；DIM 层通过配置表动态识别维表，避免把表名写死在代码里；DWD 层做明细分流和基础清洗，并通过状态关联构造订单明细事实宽流；DWS 层面向主题做窗口汇总；ADS 层基于 Doris 提供看板指标。

技术上，Flink CDC 解决了 MySQL 实时变更采集问题，Kafka 解决了层间解耦问题，HBase 解决了维度数据存储问题，Doris 解决了实时聚合查询和 ADS 展示问题。

排错上，最大的经验是不要只看异常表面。比如金额乱码问题看起来像 DWD 清洗问题，实际根因在 CDC Decimal 序列化；Doris 写入失败看起来像连接问题，实际可能是 Stream Load 数据格式问题；Doris 表没数据看起来像写入失败，实际可能是窗口没有新数据触发。

这套项目从 ODS 到 ADS 跑通后，已经具备了继续扩展的基础。后续可以继续补：

- 省份交易 DWS
- 支付主题 DWS
- 退款主题 DWS
- 流量域 DWS
- 商品维度关联
- 实时大屏接口

## 12. 常用验证 SQL

查看 DWS 数据：

```sql
SELECT *
FROM gmall_realtime.dws_trade_sku_order_window
ORDER BY stt DESC
LIMIT 20;
```

查看今日交易总览：

```sql
SELECT *
FROM gmall_realtime.ads_trade_stats_today;
```

查看 SKU Top10：

```sql
SELECT *
FROM gmall_realtime.ads_trade_sku_order_topn_today
WHERE rn <= 10
ORDER BY rn;
```

查看 30 秒趋势：

```sql
SELECT *
FROM gmall_realtime.ads_trade_trend_30s
ORDER BY stt DESC
LIMIT 20;
```

查看小时趋势：

```sql
SELECT *
FROM gmall_realtime.ads_trade_hour_stats_today
ORDER BY hour_time;
```
